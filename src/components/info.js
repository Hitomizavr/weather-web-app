import React from "react";

const Info = () => (
  <div className="infoWeather">
    <h2 className="info-we">Weather app</h2>
    <h1 className="info-we1">Find out the weather in your city!</h1>
  </div>
);

export default Info;
